with Ada.Interrupts.Names, Boption, Ada.Integer_Text_IO;
use Ada.Interrupts.Names, Boption, Ada.Integer_Text_IO;


procedure Test is

   --testHandler : Device_Interface(SIGFPE);

   testint : Integer;

begin

   Get(Testint);

   Testint := 1 mod Testint;

end Test;
